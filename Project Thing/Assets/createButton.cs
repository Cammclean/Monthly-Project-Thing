using UnityEngine;
using System.Collections;

public class createButton : MonoBehaviour {

	// Use this for initialization
	void Start () {
        GUI.Box(new Rect(0, 0, 100, 100), "I am on top");
    }
	
	// Update is called once per frame
	void Update () {
	
	}
}
